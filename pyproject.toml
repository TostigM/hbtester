[tool.poetry]
name = "hbtester-api"
version = "0.1.0"
description = ""
authors = ["TostigM"]

[tool.poetry.dependencies]
python = "^3.12"
flask = ">=3.0"
flask-cors = ">=4.0"
gunicorn = ">=21.2"
pydantic = ">=2.0"
pyyaml = ">=6.0"
click = ">=8.1"
numpy = ">=1.26"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
