# TODO

## Your Tasks

- [ ] Push `feat/m10-full-baseline` branch to remote (`git push`) so Render can see the code
- [ ] Create a free account at [render.com](https://render.com)
- [ ] New → Web Service → connect your GitHub repo → deploy (Render auto-reads `render.yaml`)
- [ ] Copy the live Render URL and paste it into `web/index.html` — find `const API_BASE = window.HB_API_BASE || '...'` and replace the placeholder
- [ ] Upload the updated `web/index.html` and `web/data/` to Bluehost

## Agent Tasks

- [ ] Re-run end-to-end API test against the live Render URL once deployed
- [ ] Verify all 8 encounter cards render correctly in the web dashboard against live data
- [ ] Open a PR from `feat/m10-full-baseline` → `master` (needs your go-ahead)
- [ ] Generate full v2.0 baseline at 1000 runs (currently 200) for publication accuracy
- [ ] Convert Legion Warlock from 2014 → 2024 rules and run it through the framework (Phase 3 primary goal — needs your direction)
